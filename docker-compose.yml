services:
  postgres:
    image: postgres:16
    container_name: iam_postgres
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: roshan123
      POSTGRES_DB: iam_db
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
